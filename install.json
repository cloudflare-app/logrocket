{
  "$schema": "http://json.schemastore.org/install",
  "resources": {
    "head": [
      {
        "type": "script",
        "src": "./src/index.js"
      }
    ]
  },
  "preview": {
    "handlers": [
      {
        "options": ["_default"],
        "execute": "INSTALL_SCOPE.setOptions(INSTALL_OPTIONS)"
      }
    ]
  },
  "options": {
    "properties": {
      "id": {
        "title": "Project ID",
        "order": 0,
        "type": "string",
        "required": true
      }
    }
  }
}
